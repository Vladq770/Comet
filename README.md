## Для запуска проекта необходимо запустить Docker и вызвать две команды из терминала в директории проекта.
## docker-compose build 
## docker-compose up -d
## Проект будет запущен по адресу http://localhost:9999/docs
